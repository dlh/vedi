The status line ends with "? help" against the right edge, so the
bindings can be found from any message. The hint is dropped, not cut,
when fewer than two spaces would separate it from the text.

-- size --
30x3
-- input --
abc
-- screen --
abc

vedi  line 1/1  wrap    ? help
-- resize --
27x3
-- screen --
abc

vedi  line 1/1  wrap
-- resize --
40x3
-- keys --
y
-- screen --
abc

nothing selected                  ? help
