The status line ends with "h help" against the right edge, so the
bindings can be found from any message. The hint is dropped, not cut,
when fewer than two spaces would separate it from the text.

-- size --
32x3
-- input --
abc
-- screen --
abc

<stdin>  line 1/1  wrap   h help
-- resize --
30x3
-- screen --
abc

<stdin>  line 1/1  wrap
-- resize --
40x3
-- keys --
y
-- screen --
abc

nothing selected                  h help
